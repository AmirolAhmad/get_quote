<div class="page-header">
  <h1>My Client <span class="badge"><%= current_user.clients.count %></span></h1>
</div>

<p class="pull-right"><%= link_to "Add New Client", new_client_path, class: "btn btn-xs btn-primary" %></p>
<table class="table table-bordered table-hover" id="datatable-metros">
  <thead>
    <tr>
      <th class="col-md-1">No</th>
      <th>Contact Person</th>
      <th>Company Name</th>
      <th class="col-md-2">Date Created</th>
      <th class="col-md-2">Action</th>
    </tr>
  </thead>
  <tbody>
    <% @clients.each_with_index do |client,index| %>
    <tr class='clickable-row' data-url="<%= client_url(client) %>">
      <td><%= index + 1 %></td>
      <td><%= client.contactPerson %></td>
      <td><%= client.companyName %></td>
      <td><%= client.created_at.strftime("%d %B %Y") %></td>
      <td>
        <div class="btn-group btn-group-justified btn-group-xs">
          <%= link_to edit_client_path(client), class: "btn btn-default", "data-toggle" => "tooltip", "data-placement" => "top", title: "Edit" do %>
            <i class="fa fa-edit"></i>
          <% end %>
          <%= link_to client_path(client), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger", "data-toggle" => "tooltip", "data-placement" => "top", title: "Delete" do %>
            <i class="fa fa-trash"></i>
          <% end %>
        </div>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
